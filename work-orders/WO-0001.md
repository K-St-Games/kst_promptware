# WO-0001 — Foundation: Make the Bundle Self-Hosting (v4 Brownfield Bootstrap)

> **Artifact class:** Work Order
> **Written by:** Planner tier (bootstrap: TD via intake conversation; revised 2026-06-12 against peer review)
> **Amended by:** Change order only, once status = `released` (free edits while `draft`)
> **Read by:** Builder at execution; Gatekeeper at closeout; Human at declared gates
> **Validated by:** `/schemas/work-order.schema.json` — *created by this WO; see Ledger L-001*

## Abstract

*Non-binding.* Establishes Boresight as v4 of the existing `AI_Agent_Dev_Docs` repository: north-star artifacts, work-order machinery, and reconciliation of the existing v3 content into a companion layer. On completion, every subsequent unit of work can be specified, executed, and verified using the system's own mechanisms.

**Status note:** transitions to date — `draft → released` 2026-06-12 (Damien's go-ahead in the planning session), `released → in-progress` 2026-06-12 (execution on branch `docs/claude-boresight-bootstrap`), `in-progress → review` 2026-06-12 (mechanical checks passed; awaiting AC-12 ratification).

---

```yaml
id: WO-0001
title: Foundation — Boresight v4 bootstrap in AI_Agent_Dev_Docs; north-star artifacts + work-order machinery
status: review           # legal: draft → released → in-progress → (blocked) → review → complete → reconciled
assignee_tier: builder   # any harness with file operations
gate: human              # sign-off required at draft→released AND review→complete
parent_feature: F-001    # BOOTSTRAP on the capability map (created by this WO — Ledger L-001)
depends_on: []
conflicts: []            # no concurrent work orders exist; bootstrap is single-threaded by decree
reads:                   # exhaustive, normative — external intake set under K_St_Games/_docs/incubator/boresight/
  - project-oneshot-overview.md      # intake: vision, principles, nomenclature
  - START-HERE.md                    # ratified intake decisions (2026-06-12)
  - htf-coder-feedback.md            # peer review — absorbed P0 items
  - antigravity-feedback.md          # peer review — absorbed P0 items
  - codex-feedback.md                # peer review — absorbed P0 items
  - work-order-template.md           # deliverable-in-progress; Step 5 starting point
discovery_roots:         # inspect for brownfield facts only — no norms may be extracted
  - ./                   # existing repo content: AGENTS.md, README.md, templates/, guides/, archive/
writes:                  # exhaustive — paths relative to this repo root
  - /charter.md
  - /constitution.md
  - /ledger.md
  - /capability-map.md
  - /templates/work-order.md
  - /schemas/work-order.schema.json
  - /LICENSE
  - /CHANGELOG.md
  - /AGENTS.md
  - /README.md
  - /templates/README.md
  - /work-orders/WO-0001.md
spawns: [WO-0002, WO-0003, WO-0004, WO-0005, WO-0006, WO-0007]
```

---

## 1. Goal

On completion: the `AI_Agent_Dev_Docs` repository carries the Boresight v4 identity, and every *subsequent* unit of work can be specified, executed, and verified using the system's own machinery. Concretely — WO-0002 can be cut from `/templates/work-order.md`, validated against `/schemas/work-order.schema.json`, and tracked on `/capability-map.md`, while the existing v3 session framework remains intact as a companion layer.

## 2. Non-Goals

This work order does **not** produce:

1. The full v0.1 bundle.
2. Playbooks or commands (WO-0004).
3. Templates other than the work-order template (WO-0002, WO-0003).
4. The full README rewrite or design-notes prose (WO-0005) — Step 9 changes identity framing only.
5. Example content (WO-0006).
6. Adapter shims for harnesses other than the existing `AGENTS.md` reconciliation (WO-0007).
7. Runtime tooling of any kind, including the Gatekeeper validator (deferred — Ledger L-007).
8. Integration or restructuring of the v3 session-layer templates beyond the companion banner (deferred — Ledger L-008).

## 3. Reads, Discovery & Writes

All three lists are closed-world. **Writes** bounds what this order may change: if a surface is not listed, this order does not create or mutate it. **Reads** bounds what this order may rely on: knowledge from outside the reads list that would shape a *norm or contract* requires a ledger entry before use. **Discovery roots** bound what may be inspected: the existing repo content supplies *facts for reconciliation* (what exists, where, in what shape) — it is not a source of norms; any v3 convention that would become binding doctrine enters through the constitution deliberately, not by inheritance.

**Reads:** the intake set — the design overview, the ratified decisions in `START-HERE.md`, the three peer reviews (whose P0 items this revision absorbs), and the work-order template draft. All live in the incubator folder at `K_St_Games/_docs/incubator/boresight/`.

**Flow:**

```
intake set (overview + ratified decisions + peer reviews)
   ├─→ charter.md + constitution.md     (north star — human-ratified)
   ├─→ capability-map.md                (decomposition + WO queue)
   ├─→ templates/work-order.md          (machinery — conformed from revised draft)
   │        └─→ schemas/work-order.schema.json
   ├─→ ledger.md                        (bootstrap exceptions + open questions)
   └─→ LICENSE + CHANGELOG.md           (repo identity)

existing repo (discovery)
   ├─→ AGENTS.md   (reconcile: pointer additions)
   ├─→ README.md   (reconcile: v4 identity)
   └─→ templates/README.md  (banner: companion layer)
```

## 4. Constraints in Force

*These constraints seeded the constitution and now bind from there.*

1. Constitution §1 (format, no build step, codename neutrality), §2 (governance headers), §3 (status canon), §4 (directory mutation rights + brownfield exception), §5 (noun budget), §6 (two-audience rule), §7 (closed-world reads/discovery/writes), §10 (host-repo conduct).
2. **Do not:** rewrite the host repo's standing rules (`guides/`, archive policy); regenerate the existing v3 templates; restructure `/templates`.

## 5. Steps

*Discretion rule: ambiguity about a **norm or contract** → ledger entry, stop or flag. Ambiguity about **phrasing or structure** → builder's discretion.*

1. Create a feature branch per `guides/contributing.md`. *(fixed)* — `docs/claude-boresight-bootstrap`
2. Create the repo additions — north-star files, bundle directories; existing directories untouched. *(fixed)*
3. Draft `charter.md`: vision, users, problem, success criteria, non-goals (≥3), codename status, copyright holder, glossary + artifact-class enumeration. *(content fixed by intake; phrasing at discretion)*
4. Draft `constitution.md`: seeded constraints + commit standards + definition of done + routing rule + transitive-write classes + reads/discovery distinction + full state machine. Lean v0 clause. *(norms fixed; structure at discretion)*
5. Conform `templates/work-order.md` from the revised v0.2.0 incubator draft. *(section set fixed; instructional wording at discretion)*
6. Create `schemas/work-order.schema.json` covering all frontmatter fields used here, codename-neutral `$id`. *(fixed)*
7. Create `capability-map.md` with the WO-0002…0007 queue; ordering provisional, ratify at gate. *(queue content fixed; format at discretion)*
8. Reconcile `AGENTS.md`: load-on-demand shape preserved; Boresight pointers added; pointers only. *(pointer set fixed; wording at discretion)*
9. Reconcile `README.md`: v4 identity framing + docs-map addition; full rewrite stays WO-0005. *(identity content fixed; structure at discretion)*
10. Add the companion-layer banner to `templates/README.md`; no restructuring. *(fixed)*
11. Create `LICENSE`: MIT, Damien O'Donnell. *(fixed)*
12. Create `CHANGELOG.md`: backfill v01 → v02 → v3 → v4. *(entries fixed; format at discretion)*
13. Seed `ledger.md` with L-001 … L-009. *(fixed)*

## 6. Acceptance Checks

- [x] AC-1 — Directory tree matches Step 2; pre-existing directories untouched. *(diffable)*
- [x] AC-2 — Every created governed artifact contains a governance header. *(grep)*
- [x] AC-3 — `work-order.schema.json` validates **this document's own frontmatter** via one-off manual run; command and result in evidence. *(runnable)*
- [x] AC-4 — No released artifact contains a `!!…!!` placeholder token (template excluded — it defines them). *(grep)*
- [x] AC-5 — Every feature on the capability map has a status; every queued WO has an id. *(lintable)*
- [x] AC-6 — `charter.md` has ≥3 non-goals and a glossary enumerating the artifact classes. *(grep)*
- [x] AC-7 — `LICENSE` is MIT and names Damien O'Donnell. *(grep)*
- [x] AC-8 — `CHANGELOG.md` contains v01, v02, v3, and v4 entries. *(grep)*
- [x] AC-9 — `ledger.md` contains entries L-001 through L-009. *(grep)*
- [x] AC-10 — Schema contains no codename occurrence in `$id` or field names. *(grep)*
- [ ] AC-11 — `AGENTS.md` remains one screen, pointers only. *(human gate: Damien, at review)*
- [ ] AC-12 — **Human gate:** charter and constitution ratified by Damien before `review → complete`.

## 7. Escalation Triggers

Standing triggers (fixed) — stop, set status `blocked`, and emit the escalation payload (§8); do not improvise — if:

1. Any step requires mutating a surface outside `writes` beyond the transitive-write classes, or relying on context outside `reads` for a norm or contract.
2. Any step requires a framework noun not resolvable by the Constitution §5 procedure.
3. The schema cannot express a frontmatter field this document uses.
4. An acceptance check fails for environment or infrastructure reasons — block as `infra_block`; do not enter the repair loop.

Order-specific:

5. Existing repo content (contributing rules, AGENTS.md conventions, archive policy) **contradicts** an intake decision in a way that pointer-reconciliation cannot resolve — ledger entry, stop at the gate. Do not silently rewrite the host repo's standing rules.

## 8. Closeout

**Artifact obligations:** this order filed at `/work-orders/WO-0001.md` (done); capability map updated (WO-0001 → `review`; queue listed); ledger seeded with L-001 … L-009 (done — see `/ledger.md` for full text).

**Evidence block** *(filled at review entry, 2026-06-12)*:

```yaml
evidence:
  commit: "branch docs/claude-boresight-bootstrap (sha recorded at PR)"
  environment: "macOS; python3 + PyYAML + jsonschema (draft-07)"
  checks:
    - id: AC-1
      method: diffable
      command: "ls -d <step-2 paths>"
      result: pass
    - id: AC-2
      method: grep
      command: "grep -q 'Written by:' <each governed artifact>; grep -q 'Governance:' schemas/work-order.schema.json"
      result: pass
    - id: AC-3
      method: runnable
      command: "python3: yaml.safe_load(frontmatter fence) → jsonschema.validate(against schemas/work-order.schema.json)"
      result: pass
    - id: AC-4
      method: grep
      command: "grep -rnE '!![A-Za-z0-9_-]+!!' <all governed artifacts except templates/work-order.md>"
      result: pass
    - id: AC-5
      method: lintable
      command: "grep -cE '^\\| F-00[0-9]' capability-map.md → 7 rows, all with status + WO id"
      result: pass
    - id: AC-6
      method: grep
      command: "5 non-goal entries; 'Artifact classes (enumeration, fixed)' present in charter.md"
      result: pass
    - id: AC-7
      method: grep
      command: "grep 'MIT License' + \"Damien O'Donnell\" LICENSE"
      result: pass
    - id: AC-8
      method: grep
      command: "grep '## v4.0.0|v3.0.0|v2.0.0|v1.0.0' CHANGELOG.md"
      result: pass
    - id: AC-9
      method: grep
      command: "grep 'L-001'..'L-009' ledger.md"
      result: pass
    - id: AC-10
      method: grep
      command: "grep -i boresight schemas/work-order.schema.json → no matches"
      result: pass
    - id: AC-11
      method: human-gate
      command: "Damien reviews AGENTS.md at the review gate"
      result: pending
    - id: AC-12
      method: human-gate
      command: "Damien ratifies charter.md + constitution.md before review → complete"
      result: pending
```

**Reconciliation block**:

- Deltas from plan:
  - `.gitkeep` files added to `/features`, `/decisions`, `/playbooks`, `/commands` — git cannot track empty directories. Logged as transitive writes.
  - `LICENSE` carries no governance header — verbatim legal text; codified as Constitution §2.2 rather than left as an unexplained exception.
  - AC-4 scope: `/templates/work-order.md` is excluded from the placeholder grep — the template *defines* the `!!…!!` convention; an initialized order containing tokens is what fails.
  - Conventional-commit policy difference: host repo forbids generated `Co-Authored-By` lines; host policy followed per Constitution §10.1.
- Transitive writes (class · file · reason): `mechanical-reference` · `features/.gitkeep`, `decisions/.gitkeep`, `playbooks/.gitkeep`, `commands/.gitkeep` · empty directories are untrackable in git.
- Follow-ups spawned: WO-0002 … WO-0007 (queued on the capability map; ordering provisional pending the AC-12 gate).
- Ledger entries added: L-001 … L-009.
- Concerns: the incubator copy of this order (in `K_St_Games/_docs/incubator/boresight/`) is now a superseded staging draft — it carries a pointer banner to this filed copy to prevent a second status surface.

**Exit condition (three-part):** all acceptance checks pass ∧ every status transition was legal ∧ closeout obligations fulfilled. Any other result exits nonzero into the repair loop (conformance failure) or a change order (spec gap). No third path. (`infra_block` is a suspension, not an exit.)
